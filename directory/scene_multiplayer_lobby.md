# scenes/multiplayer_lobby.tscn

Multiplayer lobby UI. Two panels toggle visibility: **SetupPanel** (Host / IP field / Join buttons) and **LobbyPanel** (player list + Start button, Start only visible to host). Back button returns to main menu and clears the peer. Script: `scripts/multiplayer_lobby.gd`.
