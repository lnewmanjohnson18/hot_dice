# scenes/main_menu.tscn

Title screen. Full-screen Control node with a centered VBoxContainer holding a "Hot Dice" label, a **Play** button (→ `game.tscn`), and a **Multiplayer** button (→ `multiplayer_lobby.tscn`). Script: `scripts/main_menu.gd`.
